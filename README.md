# SpendSense Web Edition

Веб-версия приложения SpendSense для анализа банковских выписок в PDF формате с поддержкой оригинального формата и RBC формата.

## Особенности

- 🔍 **Автоматическое определение формата** - поддержка оригинального и RBC форматов
- 📊 **Детальная аналитика** - статистика по участникам, категориям и периодам
- 📱 **Адаптивный дизайн** - удобный доступ с мобильных устройств
- 📈 **Визуализация данных** - таблицы и сводки
- 📄 **Экспорт данных** - выгрузка в Excel и CSV форматах
- 🔒 **Простая аутентификация** - защищенный доступ
- 🌙 **Темная тема** - современный интерфейс

## Технологический стек

### Backend
- **FastAPI** - современный веб-фреймворк
- **Python 3.10+** - язык программирования
- **pdfplumber** - парсинг PDF файлов
- **pandas** - обработка данных
- **xlsxwriter** - создание Excel файлов

### Frontend
- **React 18** - пользовательский интерфейс
- **TypeScript** - типизированный JavaScript
- **shadcn/ui** - компоненты интерфейса
- **Tailwind CSS** - стилизация
- **Vite** - сборка проекта

## Быстрый старт

### 1. Установка зависимостей

```bash
# Frontend зависимости (уже установлены)
pnpm install

# Backend зависимости
cd backend
pip install -r requirements.txt
```

### 2. Запуск приложения

**Терминал 1 - Backend сервер:**
```bash
cd backend
python start_server.py
```

**Терминал 2 - Frontend (уже запущен):**
```bash
pnpm run dev
```

### 3. Доступ к приложению

- **Веб-интерфейс**: http://localhost:5173
- **API документация**: http://localhost:8000/docs
- **Логин**: SpendSense
- **Пароль**: 12345

## Использование

1. **Войдите в систему** используя логин `SpendSense` и пароль `12345`
2. **Загрузите PDF файл** банковской выписки (поддерживаются оригинальный и RBC форматы)
3. **Дождитесь обработки** - система автоматически определит формат и извлечет данные
4. **Анализируйте результаты** - просматривайте статистику, сводки и транзакции
5. **Фильтруйте по датам** - выберите нужный период для анализа
6. **Экспортируйте данные** - скачайте результаты в Excel или CSV формате

## Архитектура

### Backend API

- `POST /api/auth/login` - аутентификация пользователя
- `POST /api/upload` - загрузка PDF файла
- `POST /api/process/{session_id}` - обработка загруженного файла
- `POST /api/export/{session_id}` - экспорт данных в Excel/CSV
- `GET /api/health` - проверка состояния сервера

### Обработка данных

Приложение использует **существующие модули** из десктопной версии без изменений:

- `unified_parser.py` - парсинг PDF с автоматическим определением формата
- `excel_writer.py` - создание Excel файлов с множественными листами
- `csv_writer.py` - экспорт в универсальный CSV формат

### Безопасность

- JWT токены для аутентификации
- Временное хранение файлов с автоматической очисткой
- Валидация типов файлов
- CORS настройки для кросс-доменных запросов

## Поддерживаемые форматы

### Оригинальный формат
- Транзакции с участниками (JASON DIMAND, GRIGORII VOLK)
- Поддержка валютных операций
- Гибкие платежи (FS)

### RBC формат
- Упрощенная структура транзакций
- Информация о стране продавца
- Валютные операции с курсами обмена

## Разработка

### Структура проекта

```
/
├── backend/                 # Backend сервер
│   ├── main.py             # FastAPI приложение
│   ├── start_server.py     # Скрипт запуска
│   ├── unified_parser.py   # Парсер PDF (из десктопной версии)
│   ├── excel_writer.py     # Excel экспорт (из десктопной версии)
│   ├── csv_writer.py       # CSV экспорт (из десктопной версии)
│   ├── uploads/            # Временные загрузки
│   └── exports/            # Временные экспорты
├── src/                    # Frontend приложение
│   ├── components/         # React компоненты
│   ├── lib/               # Утилиты и API клиент
│   └── App.tsx            # Главный компонент
└── public/                # Статические файлы
    └── assets/            # Изображения
```

### Команды разработки

```bash
# Проверка кода
pnpm run lint

# Сборка проекта
pnpm run build

# Предварительный просмотр сборки
pnpm run preview
```

## Особенности реализации

- **Без изменения парсеров** - используются существующие модули из десктопной версии
- **Серверная обработка** - все вычисления происходят на бэкенде
- **Прогрессивная загрузка** - отображение прогресса обработки
- **Адаптивный интерфейс** - оптимизация для мобильных устройств
- **Автоматическая очистка** - удаление временных файлов

## Лицензия

© 2025 SpendSense Web Edition - специальная онлайн версия для удобного доступа